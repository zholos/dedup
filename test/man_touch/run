dedup -rfx 'touch -r "$2" -- "$1"' b a
( find b -printf '%p %Ts\n' 2>/dev/null ||
  find b -exec stat -f '%N %m' {} \; ) | sort
echo ---
dedup -rfx 'mv -n -- "$1" "${1%/*}/${2##*/}" || :' b a
find b | sort
